"""
como buenas practicas primero debemos declarar las funciones luego las variables, y luego el call
a las funciones y el uso de funciones predefinidas.

Tambien, las funciones siempre tienen que tener un return y en lugar de colocar un print dentro de 
la funcion, que la funcion retorne lo que tenga que retornar y hacerle un print a la funcion, de 
modo que con return le asignamos un valor a la funcion, y con print la mostramos, y no que la funcion
haga el print en si. 

las variables deben ser definidas antes de ser utilizadas
"""

















